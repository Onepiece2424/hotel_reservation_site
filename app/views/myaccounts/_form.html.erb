<%= form_with(model: myaccount) do |form| %>
  <% if myaccount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(myaccount.errors.count, "error") %> prohibited this myaccount from being saved:</h2>

      <ul>
        <% myaccount.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>

  <div class="field">
    <%= form.label :introduction %>
    <%= form.text_area :introduction %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
