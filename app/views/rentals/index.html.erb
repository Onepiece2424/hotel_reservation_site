<p id="notice"><%= notice %></p>

<h2>借りたい物件の登録・予約履歴</h2>

<table>
  <thead>
    <tr>
      <th>レンタル物件名</th>
      <th>宿泊日数</th>
      <th>宿泊料金</th>
      <th>人数</th>
      <th>合計料金(宿泊日数 × 宿泊料金 × 人数)</th>
      <th colspan="3">参照・編集・削除</th>
    </tr>
  </thead>

  <tbody>
    <% @rentals.each do |rental| %>
      <tr>
        <td><%= rental.hotelname %></td>
        <td><%= rental.staydays %></td>
        <td><%= rental.hotelfee %></td>
        <td><%= rental.numberofpeple %></td>
        <td><%= rental.pricesum %></td>
        <td><%= link_to 'Show', rental %></td>
        <td><%= link_to 'Edit', edit_rental_path(rental) %></td>
        <td><%= link_to 'Destroy', rental, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
  
</table>

<br>

<%= link_to '借りたい物件を登録する', new_rental_path %>
<br>
<%= link_to 'Topページ画面へ', projects_index_path %>